\name{d2BC}
\alias{d2BC}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{The second derivative of the Bhattacharyya coefficient
%%  ~~function to do ... ~~
}
\description{Computes the second derivative of the Bhattacharyya coefficient between two normal densities given a matrix with descriptive statistics. The matrix contains the means and standard deviations for all the parameters in a Bayesian NNHM or NtHM.   
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
d2BC(descr_collect, hh)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{descr_collect}{matrix with descritive statistics generated by the descr_extract function
%%     ~~Describe \code{descr_collect} here~~
}
  \item{hh}{numerical differentiation step
%%     ~~Describe \code{hh} here~~
}
}
\details{See  Roos et al. (2020) for details. 
%%  ~~ If necessary, more details than the description above ~~
}
\value{A vector with the second derivative of BC.
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{Roos, M., Hunanyan, S., Bakka, H., Rue, H. (2020). Sensitivity and identification quantification by a relative latent model complexity perturbation in the Bayesian meta-analysis. Biometrical Journal (under revision). 
%% ~put references to the literature/web site here ~
}
%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{\code{\link{descr_extract}}
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
%%\examples{
%%##---- Should be DIRECTLY executable !! ----
%%##-- ==>  Define data, use random,
%%##--	or do  help(data=index)  for the standard data sets.
%%
%%## The function is currently defined as
%%function (descr_collect, hh) 
%%{
%%    nr <- nrow(descr_collect)
%%    d2BC <- rep(NA, nr)
%%    for (i in 1:nr) {
%%        d2BC[i] <- (BC_normal_total(m1 = descr_collect[i, "m_u"], 
%%            sd1 = descr_collect[i, "sd_u"], m0 = descr_collect[i, 
%%                "m_0"], sd0 = descr_collect[i, "sd_0"]) - 2 + 
%%            BC_normal_total(m1 = descr_collect[i, "m_l"], sd1 = descr_collect[i, 
%%                "sd_l"], m0 = descr_collect[i, "m_0"], sd0 = descr_collect[i, 
%%                "sd_0"]))/hh^2
%%    }
%%    return(d2BC)
%%  }
%%}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
%%\keyword{ ~kwd1 }% use one of  RShowDoc("KEYWORDS")
%%\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
